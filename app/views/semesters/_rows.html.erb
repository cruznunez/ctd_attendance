<div class="flex-container flex-column">
  <% @semesters.each do |semester| %>
    <%= link_to semester.name, course_semester_path(semester.course_id, semester), class: 'clickable-row' %>
  <% end %>
</div>
